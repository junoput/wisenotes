services:
  wisenotes:
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    volumes:
      - ./:/app:delegated
      - ./data:/data
    environment:
      - WISENOTES_DATA_DIR=/data
    ports:
      - "8000:8000"
